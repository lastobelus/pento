defmodule Pento.PromoTest do
  @moduledoc false
  
  use ExUnit.Case

  alias Pento.Promo

  describe "Pento.Promo" do
    test "write some tests" do
      flunk("no tests for #{Pento.Promo} yet!")
    end
  end
end
