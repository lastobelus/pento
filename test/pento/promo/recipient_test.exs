defmodule Pento.Promo.RecipientTest do
  @moduledoc false
  
  use ExUnit.Case

  alias Pento.Promo.Recipient

  describe "Pento.Promo.Recipient" do
    test "write some tests" do
      flunk("no tests for #{Pento.Promo.Recipient} yet!")
    end
  end
end
