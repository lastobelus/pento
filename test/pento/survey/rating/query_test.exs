defmodule Pento.Survey.Rating.QueryTest do
  @moduledoc false
  
  use ExUnit.Case

  alias Pento.Survey.Rating.Query

  describe "Pento.Survey.Rating.Query" do
    test "write some tests" do
      flunk("no tests for #{Pento.Survey.Rating.Query} yet!")
    end
  end
end
