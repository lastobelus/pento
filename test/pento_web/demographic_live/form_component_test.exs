defmodule PentoWeb.DemographicLive.FormComponentTest do
  @moduledoc false
  
  use ExUnit.Case

  alias PentoWeb.DemographicLive.FormComponent

  describe "PentoWeb.DemographicLive.FormComponent" do
    test "write some tests" do
      flunk("no tests for #{PentoWeb.DemographicLive.FormComponent} yet!")
    end
  end
end
