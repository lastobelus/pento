defmodule PentoWeb.DemographicLive.ShowComponentTest do
  @moduledoc false
  
  use ExUnit.Case

  alias PentoWeb.DemographicLive.ShowComponent

  describe "PentoWeb.DemographicLive.ShowComponent" do
    test "write some tests" do
      flunk("no tests for #{PentoWeb.DemographicLive.ShowComponent} yet!")
    end
  end
end
