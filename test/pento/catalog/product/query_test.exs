defmodule Pento.Catalog.Product.QueryTest do
  @moduledoc false
  
  use ExUnit.Case

  alias Pento.Catalog.Product.Query

  describe "Pento.Catalog.Product.Query" do
    test "write some tests" do
      flunk("no tests for #{Pento.Catalog.Product.Query} yet!")
    end
  end
end
