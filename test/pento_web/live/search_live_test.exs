defmodule PentoWeb.SearchLiveTest do
  @moduledoc false
  
  use ExUnit.Case

  alias PentoWeb.SearchLive

  describe "PentoWeb.SearchLive" do
    test "write some tests" do
      flunk("no tests for #{PentoWeb.SearchLive} yet!")
    end
  end
end
