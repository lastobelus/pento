defmodule Pento.Catalog.SearchTest do
  @moduledoc false
  
  use ExUnit.Case

  alias Pento.Catalog.Search

  describe "Pento.Catalog.Search" do
    test "write some tests" do
      flunk("no tests for #{Pento.Catalog.Search} yet!")
    end
  end
end
