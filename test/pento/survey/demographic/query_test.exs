defmodule Pento.Survey.Demographic.QueryTest do
  @moduledoc false
  
  use ExUnit.Case

  alias Pento.Survey.Demographic.Query

  describe "Pento.Survey.Demographic.Query" do
    test "write some tests" do
      flunk("no tests for #{Pento.Survey.Demographic.Query} yet!")
    end
  end
end
